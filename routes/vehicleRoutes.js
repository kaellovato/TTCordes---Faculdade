// ==================== ROTAS: CUSTOMERS ====================
// Rotas CRUD para clientes da TTCordes

const express = require('express');
const router = express.Router();

// TODO
// - Importar customerController
// - Importar authMiddleware, roleMiddleware
// - GET /
// - GET /:id
// - POST / (manager only)
// - PATCH /:id (manager only)
// - DELETE /:id (manager only)

module.exports = router;

