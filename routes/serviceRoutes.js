// ==================== ROTAS: INSTRUMENTS ====================
// Rotas CRUD para o catálogo de instrumentos musicais da TTCordes

const express = require('express');
const router = express.Router();

// TODO
// - Importar instrumentController
// - Importar authMiddleware, roleMiddleware
// - GET /
// - GET /:id
// - POST / (manager only)
// - PATCH /:id (manager only)
// - DELETE /:id (manager only)

module.exports = router;

